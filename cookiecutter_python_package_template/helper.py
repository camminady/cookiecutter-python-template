def make_sound(name: str, sound: str) -> str:
    """Let the animal make some noise.

    Parameters
    ----------
    name : str
        Name of the Animal.
    sound : str
        Sound of the Animal.

    Returns
    -------
    str
        A combined string.
    """
    return f"The {name} says {sound}."
